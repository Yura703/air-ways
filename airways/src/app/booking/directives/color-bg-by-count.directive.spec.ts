import { ColorBgByCountDirective } from './color-bg-by-count.directive';

describe('ColorBgByCountDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorBgByCountDirective();
    expect(directive).toBeTruthy();
  });
});
